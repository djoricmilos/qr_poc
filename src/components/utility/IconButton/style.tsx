import { BORDER_RADIUS, SPACING } from '@common/styles';
import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
    wrapper: {
        backgroundColor: 'rgba(255,255,255,0.1)',
        padding: SPACING.s,
        borderRadius: BORDER_RADIUS.s,
    },
});
